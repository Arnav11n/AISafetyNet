## Packages
recharts | For data visualization in the Scam Radar dashboard
framer-motion | For smooth page transitions and interactive animations
clsx | Utility for conditional class names
tailwind-merge | Utility for merging Tailwind classes

## Notes
Chatbot uses SSE (Server-Sent Events) over POST for real-time streaming.
Deepfake detection uses FormData for file uploads.
Auth is handled via Replit Auth integration (/api/login, /api/logout).
